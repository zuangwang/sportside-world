# sprtside_world

`sprtside_world` is a reserved project workspace. Its project-specific purpose,
language, implementation, and dependencies are intentionally left undecided for
the time being.

## Layout

- `src/`: reserved for future maintained source code; currently empty.
- `results/`: reserved for future durable results; currently empty.
- `agents/`: agent instructions, task briefs, reports, and relay artifacts.

## Requirements and Setup

No runtime, language version, dependencies, or setup steps have been selected.

## Run

There is no project run command until an implementation is added.

## Validation

For the blank scaffold, verify that the reserved source and result locations
exist:

```bash
test -d src && test -d results
```

## Results

No results exist yet. Future durable results belong in `results/` and must not
be silently deleted or overwritten. Any future regenerable or large artifacts
must be documented before they are ignored.

Before making changes, read the repository [agent instructions](AGENTS.md).
